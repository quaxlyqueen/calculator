# calculator
A simple calculator written in Python for the GUI and using eva for computation.

# Installation:
`git clone https://github.com/quaxlyqueen/calculator`<br>
`cd calculator`<br>
`sudo ./install`<br>
